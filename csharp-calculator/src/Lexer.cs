/**
 * Lexer for artimatic calculations.
 */

namespace Calculator {
    public class Lexer {
        private string expr;

        /** 
         * Constructor that takes in the expression for the lexer to tokenize.
         */
        public Lexer(string expression) => expr = expression;

        // TODO(kmidkiff): Add the rest of the member functions
    }
}